name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Build extension
        run: bun run build

      - name: Get version from tag
        id: version
        run: echo "version=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT

      - name: Create release asset directory
        run: mkdir -p release-assets

      - name: Pack CRX
        if: ${{ secrets.CHROME_EXTENSION_KEY != '' }}
        run: |
          printf '%s' '${{ secrets.CHROME_EXTENSION_KEY }}' > key.pem
          npx crx pack dist -p key.pem -o release-assets/xpaper-${{ steps.version.outputs.version }}.crx

      - name: Create ZIP for Chrome Web Store
        run: |
          cd dist
          zip -r ../release-assets/xpaper-${{ steps.version.outputs.version }}-chrome-webstore.zip .

      - name: Remove temporary key
        if: ${{ always() }}
        run: rm -f key.pem

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: release-assets/*
          generate_release_notes: true
          token: ${{ secrets.GITHUB_TOKEN }}
